#include<stdlib.h>
#include<time.h>
#include<stdio.h>

/**
 *main - prints all single digit numbers
 *
 *print number with new line
 *
 *Return: o
 */
int main(void)
{
	int onten;

	for (onten = 48 ; onten <= 57 ; onten++)
	{
		putchar(onten);
	}
	putchar('\n');
	return(0);
}
