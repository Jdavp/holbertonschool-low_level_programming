#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - print all single digits of base 10
 *
 *print numbers
 *
 *Return: its 0
 */
int main(void)
{
	int onten;

	for(onten = 0 ; onten <= 9 ; onten++)
	{
		printf("%d", onten);
	}
	printf("\n");
	return (0);
}
